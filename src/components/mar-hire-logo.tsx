import * as React from 'react';

export const MarHireLogo = (props: React.HTMLAttributes<HTMLSpanElement>) => (
  <span
    className="text-2xl font-bold text-primary font-headline"
    {...props}
  >
    MarHire
  </span>
);
